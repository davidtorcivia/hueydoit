.git
.gitignore
.github

# Installed inside the frontend build stage; shipping it as context is 17MB per build
frontend/node_modules
frontend/dist

# Bind-mounted at runtime, never baked into the image
data

# Not needed in the runtime image
tests
pytest.ini
__pycache__
**/__pycache__
*.pyc
*.pyo

.env
*.png
*.md
config.json
config.backup*.json
